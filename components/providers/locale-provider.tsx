/**
 * Re-export locale context for components that import from this path.
 * The real implementation lives in @/lib/i18n/locale-context.
 */
export { LocaleProvider, useLocale as useLocaleContext } from "@/lib/i18n/locale-context";
export type { } from "@/lib/i18n/locale-context";
